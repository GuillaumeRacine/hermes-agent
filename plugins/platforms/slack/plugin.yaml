name: slack-platform
label: Slack
kind: platform
version: 1.0.0
description: >
  Slack gateway adapter for Hermes Agent.
  Connects to Slack via slack-bolt in Socket Mode and relays messages
  between Slack channels/DMs and the Hermes agent. Supports slash
  commands, threads, mrkdwn rendering, approval blocks, free-response
  channels, mention gating, and channel skill bindings.
author: NousResearch
requires_env:
  - name: SLACK_BOT_TOKEN
    description: "Slack bot token (xoxb-...)"
    prompt: "Slack Bot Token (xoxb-...)"
    url: "https://api.slack.com/apps"
    password: true
  - name: SLACK_APP_TOKEN
    description: "Slack app-level token for Socket Mode (xapp-..., scope connections:write)"
    prompt: "Slack App Token (xapp-...)"
    url: "https://api.slack.com/apps"
    password: true
optional_env:
  - name: SLACK_ALLOWED_USERS
    description: "Comma-separated Slack member IDs allowed to talk to the bot"
    prompt: "Allowed users (comma-separated)"
    password: false
  - name: SLACK_ALLOW_ALL_USERS
    description: "Allow any Slack user to trigger the bot (dev only)"
    prompt: "Allow all users? (true/false)"
    password: false
  - name: SLACK_HOME_CHANNEL
    description: "Default channel ID for cron / notification delivery (starts with C)"
    prompt: "Home channel ID"
    password: false
  - name: SLACK_HOME_CHANNEL_NAME
    description: "Display name for the Slack home channel"
    prompt: "Home channel display name"
    password: false
  - name: SLACK_REACTION_FEEDBACK_CHANNELS
    description: "Comma-separated channel IDs where configured bot-message reactions become durable feedback"
    prompt: "Reaction feedback channels"
    password: false
  - name: SLACK_REACTION_FEEDBACK_USERS
    description: "Comma-separated member IDs whose thumbs-up/down reactions are accepted as feedback"
    prompt: "Reaction feedback users"
    password: false
  - name: SLACK_REACTION_FEEDBACK_STATE
    description: "Optional JSON state path for durable reaction feedback"
    prompt: "Reaction feedback state path"
    password: false
  - name: SLACK_REACTION_FEEDBACK_MARKER
    description: "Marker text required on a bot message before its reactions are recorded"
    prompt: "Reaction feedback marker"
    password: false
